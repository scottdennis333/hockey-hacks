let mapTeam,mapRoster,extractCallback;_dac‍.x([["default",()=>_dac‍.o]]);_dac‍.w("../helpers/teamHelper.mjs",[["mapTeam",["mapTeam"],function(v){mapTeam=v}],["mapRoster",["mapRoster"],function(v){mapRoster=v}]]);_dac‍.w("../helpers/argsParser.mjs",[["extractCallback",["extractCallback"],function(v){extractCallback=v}]]);



class RosterResource {
  constructor(yf) {
    this.yf = yf;
  }

  players(teamKey, ...args) {
    let url = `https://fantasysports.yahooapis.com/fantasy/v2/team/${teamKey}/roster`;
    const cb = extractCallback(args);

    if (args.length) {
      let date = args.pop();
      if (date.indexOf("-") > 0) {
        // string is date, of format y-m-d
        url += `;date=${date}`;
      } else {
        // number is week...
        url += `;week=${date}`;
      }
    }

    return this.yf
      .api(this.yf.GET, url)
      .then((data) => {
        const team = mapTeam(data.fantasy_content.team[0]);
        const roster = mapRoster(data.fantasy_content.team[1].roster);
        team.roster = roster;
        cb(null, team);
        return team;
      })
      .catch((e) => {
        cb(e);
        throw e;
      });
  }
}

_dac‍.d(RosterResource);
